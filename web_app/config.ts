export const AppConfig = {
  // Base URL for the Docker API. 
  // An empty string means it will use a relative path from the current host.
  // Example: 'http://localhost:8000'
  API_BASE_URL: '/api/v1', 
};
